---
apiVersion: v1
kind: Pod
metadata:
  name: frontend
spec:
  selector:
     app: flipkart
  containers:
  - name: app
    image: 
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
